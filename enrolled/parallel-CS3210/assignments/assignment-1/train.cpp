#include<bits/stdc++.h>
using namespace std;

int main() {
    int n; cin >> n;
    int dist[n][n]
    for (int i = 0; i < n; i++)
        cin >> station[i];
    for (int i = 0; i < n; i++) {
        for (int i = 0; i < n; i++) {
            cin >> dist[i][j];
        }
    }
}
